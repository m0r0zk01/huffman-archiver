#include "../compressor.h"
#include "../decompressor.h"

#include <gtest/gtest.h>

TEST(TestArchvier, ArchiveSingleFile) {
    ASSERT_EQ(1, 1);
}

TEST(TestArchiver, ArchiveMultipleFiles) {

}